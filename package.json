{
  "name": "nextjs-common-host-redirect-issue",
  "version": "0.0.1",
  "description": "Reproduce issue within NextJS where a redirect to a external URL on a common host fails.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Chris Frank <chris@cfrank.org>",
  "license": "BSD-3-Clause"
}
